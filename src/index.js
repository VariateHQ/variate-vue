import { install } from './variate';
import { mapAttributes, mapStyles } from './helpers';

export {
    mapAttributes,
    mapStyles
}

export default {
    install,
    mapAttributes,
    mapStyles,
}
