import { install } from './variate';
import { mapVariables, mapStyles } from './helpers';

export {
    mapVariables,
    mapStyles
}

export default {
    install,
    mapVariables,
    mapStyles,
}
